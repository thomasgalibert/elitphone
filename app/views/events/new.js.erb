$("html, body").animate({ scrollTop: 0 }, "slow");
$(".title-canvas").html("<%= t('event.new') %>");
$(".content-canvas").html("<%= j render('events/form') %>");
$("#offcanvas").addClass('target');
removeClick();
openRecurrentDialog();

